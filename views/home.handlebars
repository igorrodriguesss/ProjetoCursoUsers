<h1>Usuários Cadastrados</h1>
<div class="container">
    {{#each users}}
        <div class="item">
            <p>{{this.name}}</p>
            <p>{{this.occupation}}</p>
            <a href="/users/{{this.id}}">Ver Detalhes</a>
        </div>
    {{/each}}
</div>